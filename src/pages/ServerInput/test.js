import React from 'react'
import { FetchCoinData, } from './modules'

import renderer from 'react-test-renderer'

describe('ServerInput', () => {
  it.only('should fetch coin data', async() => {
    expect(false).toBeTruthy()
  })
})
